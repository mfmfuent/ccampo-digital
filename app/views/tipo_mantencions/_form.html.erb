<%= form_with(model: tipo_mantencion) do |form| %>
  <% if tipo_mantencion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipo_mantencion.errors.count, "error") %> prohibited this tipo_mantencion from being saved:</h2>

      <ul>
        <% tipo_mantencion.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
