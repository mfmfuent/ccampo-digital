<%= form_with(model: cultivo) do |form| %>
  <% if cultivo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cultivo.errors.count, "error") %> prohibited this cultivo from being saved:</h2>

      <ul>
        <% cultivo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :fitosanitarios_id %>
    <%= form.text_field :fitosanitarios_id %>
  </div>

  <div class="field">
    <%= form.label :actividads_id %>
    <%= form.text_field :actividads_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
