<%= form_with(model: campo) do |form| %>
  <% if campo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(campo.errors.count, "error") %> prohibited this campo from being saved:</h2>

      <ul>
        <% campo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :superficie %>
    <%= form.text_field :superficie %>
  </div>

  <div class="field">
    <%= form.label :explotacions_id %>
    <%= form.text_field :explotacions_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
