<p id="notice"><%= notice %></p>

<h1>Campos</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Superficie</th>
      <th>Explotacions</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @campos.each do |campo| %>
      <tr>
        <td><%= campo.nombre %></td>
        <td><%= campo.superficie %></td>
        <td><%= campo.explotacions_id %></td>
        <td><%= link_to 'Show', campo %></td>
        <td><%= link_to 'Edit', edit_campo_path(campo) %></td>
        <td><%= link_to 'Destroy', campo, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Campo', new_campo_path %>
