<%= form_with(model: tipo_maquinarium) do |form| %>
  <% if tipo_maquinarium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipo_maquinarium.errors.count, "error") %> prohibited this tipo_maquinarium from being saved:</h2>

      <ul>
        <% tipo_maquinarium.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_field :descripcion %>
  </div>

  <div class="field">
    <%= form.label :maquinaria_id %>
    <%= form.text_field :maquinaria_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
