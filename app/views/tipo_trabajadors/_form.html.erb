<%= form_with(model: tipo_trabajador) do |form| %>
  <% if tipo_trabajador.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipo_trabajador.errors.count, "error") %> prohibited this tipo_trabajador from being saved:</h2>

      <ul>
        <% tipo_trabajador.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
